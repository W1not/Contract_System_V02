<Window x:Class="Sistema_de_Contratos.Vistas.GestionPuestosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sistema_de_Contratos.Vistas"
        mc:Ignorable="d"
        Title="Gestión de Puestos" 
        Height="500" 
        Width="800"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#00AFB9"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#0098B0"/>
        <SolidColorBrush x:Key="PrimaryLightColor" Color="#0081A7"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#008DAC"/>

        <!-- Estilos reutilizables -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="0 0 10 0"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Margin" Value="0 0 0 15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8 5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado con efecto hover -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" CornerRadius="4" Padding="15" Margin="0 0 0 15">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="GESTIÓN DE PUESTOS" 
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Bold"/>
        </Border>

        <!-- Barra de herramientas -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 15">
            <Button x:Name="btnAgregar" Content="Agregar" Click="BtnAgregar_Click"/>
            <Button x:Name="btnEditar" Content="Editar" Click="BtnEditar_Click"/>
            <Button x:Name="btnEliminar" Content="Eliminar" Click="BtnEliminar_Click"/>
            <Button x:Name="btnActualizar" Content="Actualizar" Click="BtnActualizar_Click"/>
        </StackPanel>

        <!-- Lista de puestos -->
        <DataGrid x:Name="dgPuestos" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdPuesto}" Width="80"/>
                <DataGridTextColumn Header="NOMBRE DEL PUESTO" Binding="{Binding Nombre}" Width="*"/>
                <DataGridCheckBoxColumn Header="ACTIVO" Binding="{Binding Activo}" Width="100">
                    <DataGridCheckBoxColumn.ElementStyle>
                        <Style TargetType="CheckBox">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="IsEnabled" Value="False"/>
                        </Style>
                    </DataGridCheckBoxColumn.ElementStyle>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Formulario para agregar/editar -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="0 15 0 0" 
                Visibility="Collapsed" x:Name="pnlFormulario">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                    <ColumnDefinition Width="*" MinWidth="300"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Nombre del puesto:" Grid.Row="0" Grid.Column="0"/>
                <TextBox x:Name="txtNombre" Grid.Row="0" Grid.Column="1"/>

                <CheckBox x:Name="chkActivo" Content="Puesto activo" Grid.Row="1" Grid.Column="1"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnCancelar" Content="Cancelar" Click="BtnCancelar_Click"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button x:Name="btnGuardar" Content="Guardar" Click="BtnGuardar_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>